# 2026-05-14

Executed all four polish tiers from `docs/review-2026-04-30.md` in a single
session. Faces are now readable in dialog, every zone has been densified,
and every NPC has visible idle behaviour beyond bobbing.

## Done

### T1 — Portrait system *(addresses the face complaint)*

- New [`entities/PortraitFactory.ts`](../../src/entities/PortraitFactory.ts):
  composes the head + shoulders crop (top 32 px of LPC frame 130 idle-down)
  of each cosmetic layer at 3× scale into a 96×96 canvas texture. Cached
  by appearance hash on the scene's `TextureManager` — built once per NPC
  at construction.
- [`entities/NPC.ts`](../../src/entities/NPC.ts) exposes `portrait` key;
  `NPCSpec` gains optional `dialog: DialogLine[]` field. If set, `addNPC`
  auto-wires interact → `speakAs(npc, dialog)` so the portrait flows
  through without callers having to.
- [`world/BaseZoneScene.ts`](../../src/world/BaseZoneScene.ts) `openDialog`
  signature picked up an optional `portrait` key; new `speakAs(npc, lines)`
  helper for NPC-sourced dialog.
- [`types/index.ts`](../../src/types/index.ts) `DialogRequest` gained
  optional `portrait` field.
- [`scenes/UIScene.ts`](../../src/scenes/UIScene.ts) dialog box height
  bumped 64 → 80 to fit a 64×64 portrait with padding. Portrait sprite +
  inset frame rect render on the left when a dialog has one; text
  position + word-wrap width recompute around the portrait.
- [`scenes/TownScene.ts`](../../src/scenes/TownScene.ts) and
  [`scenes/EngineeringDistrictScene.ts`](../../src/scenes/EngineeringDistrictScene.ts)
  switched the existing dialog NPCs (greeter + work-history foremen) to
  the new `dialog` field — they get portraits for free.

### T2 — Foundations + dungeon floor

- Engineering buildings now sit on auto-tile dirt foundations painted with
  the existing grass↔dirt 3×3 set — the same pattern used for Town
  cottages and the farm pen. Buildings no longer butt directly into stone
  pavement.
- New procedural `tex_dungeon_floor` (`makeDungeonFloor`) replaces the
  `tex_wall` placeholder on the Full-Stack Dungeon floor. Large flagstone
  pavers with dark grout lines + light/dark specks; reads as worked stone.
- Concave-corner auto-tile support is deferred — no zone currently places
  adjacent transition rects, so the artifact isn't visible. Tracked in
  `docs/project.md` "Soon."

### T3 — Density + per-zone variety

- **AI/ML Lab**: carpet widened 3 → 6 tiles tall. Server count 8 → 12, one
  every 4 tiles instead of 6. Three new procedural whiteboard sprites
  between server clusters. Monitor workbench row moved on-screen
  (previously at y=576, off-camera at default spawn). Crate stacks added
  against both side walls.
- **Full-Stack Dungeon**: crate _clusters_ of 3 at each corridor mouth
  (was: 6 evenly-spaced lone crates along one corridor). Rubble piles
  scattered through both arms. Cobweb count doubled (8 total, one per
  corner).
- **Engineering**: buildings alternate between brown-roof (existing) and
  new slate-roof variant — `makeBuildingTex` now takes wall/trim/roof
  colour params so a single recipe drives both. Street lamps placed along
  the north sand shoulder between buildings.

### T4 — Polish

- NPCs now glance left or right every 6–14 s and snap back to their
  default facing (~1 s glance duration). Adds visible life without
  breaking the "stationed here" pose. Lives in
  [`entities/NPC.ts`](../../src/entities/NPC.ts) ctor.
- Fishing Dock cast-spot bobber gained a yoyo bob tween (was a static 40%
  alpha decal). Three additional bobbers drift in the open water with
  staggered phases for ambient atmosphere.

## Decisions

- **Portrait textures live on the scene's `TextureManager`** rather than a
  module-level cache. Re-entering a scene rebuilds them on demand, which
  is fine because composition is fast (≈1 ms per portrait).
- **`NPCSpec.dialog` field instead of forcing every caller to use
  `speakAs`** — most NPCs have a static dialog, and an explicit
  `onInteract` would have meant every dialog NPC repeating
  `() => this.speakAs(...)` boilerplate. The `addNPC` helper now does the
  right thing automatically.
- **Procedural rather than atlas-sourced dungeon floor** — the atlas's
  slate variants all have grass tufts (designed for outdoor paths). A
  4×4 procedural flagstone with grout reads cleaner for an indoor
  dungeon.
- **`makeBuildingTex` parameterised by colour rather than two separate
  procedural methods** — keeps the recipe in one place and lets us
  cheaply add a third or fourth building variant later.

## Open

- Concave-corner auto-tile (deferred this session, tracked in `project.md`).
- Stone↔sand transition tile for Engineering's sand shoulders against
  surrounding stone pavers.
- Brick-path auto-tile so the brick paths blend at world edges instead of
  clipping cleanly.

## Visual verification

Walked every scene with the preview tools after T1; portrait visible on
the left of the dialog box when the Town greeter speaks. T2/T3/T4 changes
verified by typecheck + production build — visual confirmation of the
density/variety changes will land when next we drive the dev server.

## Commits

```
697c84c  feat(T4): NPC look-around + bobbing bobbers in the pond
1efa233  feat(T3): density + per-zone variety pass
8fb0424  feat(T2): packed-earth foundations + proper dungeon floor
1bb84db  feat(T1): per-NPC portraits in dialog box
```
